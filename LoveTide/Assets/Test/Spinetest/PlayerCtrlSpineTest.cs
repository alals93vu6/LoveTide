using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Spine.Unity;
public class PlayerCtrlSpineTest : MonoBehaviour
{
    [SerializeField] public SkeletonAnimation actorAN;
    [SerializeField] public SkeletonAnimation swordAN;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
