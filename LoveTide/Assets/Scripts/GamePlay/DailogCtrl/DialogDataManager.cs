using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DialogDataManager : MonoBehaviour
{
    [SerializeField] private DialogDataDetected dialogDataDetected;
    [SerializeField] private DiaDataItem diaDataItem;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
